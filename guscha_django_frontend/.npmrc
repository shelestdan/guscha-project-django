# NPM Security Configuration
audit-level=moderate
fund=false
legacy-peer-deps=true

# Security: Block known vulnerable packages
# This prevents installation of packages with high vulnerabilities
# Use with caution in production environments 